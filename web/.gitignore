# Env / secrets
.env
.env.*
!.env.example
frontend/.env.local

# Python
__pycache__/
*.py[codz]
.venv/
.ruff_cache/
.pytest_cache/
.mypy_cache/
*.egg-info/

# Node / Next
node_modules/
.next/
next-env.d.ts

# uv
# uv.lock IS committed

# misc
.DS_Store

# Artefatos gerados (PDFs/ZIPs de despachos do módulo CCD)
backend/.artifacts/

# O .gitignore da raiz tem um `lib/` amplo que captura o src/lib/ do frontend
# Next.js. Reincluímos a árvore (precisa negar o diretório antes dos arquivos).
!frontend/src/lib/
!frontend/src/lib/**
